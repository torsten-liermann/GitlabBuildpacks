deploy:docker:
  image: "$CNCF_BUILDER_IMAGE"
  stage: push
  extends: [ ".registry-auth-for-docker" ]
  artifacts:
    name: "report"
    paths:
      - report.toml
  cache: []
  script:
    - /cnb/lifecycle/creator -platform ./platform -log-level debug -app . -cache-image $APP_IMAGE_NAME/paketo-build-cache $APP_IMAGE_NAME


